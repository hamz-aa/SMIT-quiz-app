import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  filterInput: {
    maxHeight: "36px",
  },
});

export const MenuProps = {
  PaperProps: {
    style: {
      maxHeight: 224,
      width: 250,
    },
  },
};
